<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>Facture</title>
    <link href="{{ asset('css/facture.css') }}" rel="stylesheet" type="text/css" />
</head>

<body>
<h1 style="text-align: center">E-Shop!</h1>

    <h2 style="color: orangered;text-align: center">Facture</h2>

    <div>
        <p>Date :<span class="color">{{ facture.date|date('d-m-Y') }}</span></p>
        <p>Ref :<span class="color">{{ facture.reference }}</span></p>

        <strong style="color: orangered">Coordonnées</strong>
        <p>Nom :<span class="color">{{ facture.commande.facturation.nom }} {{ facture.commande.facturation.prenom }}</span></p>
        <p>Adresse :<span class="color">{{ facture.commande.facturation.adresse}} , {{ facture.commande.facturation.cp }} {{ facture.commande.facturation.ville }} - {{ facture.commande.facturation.pays }}</span></p>

        <br>
        <br>
    </div>

    <table id="entity">
        <thead>
            <tr>
                <td>DESIGNATION</td>
                <td>QUANTITE</td>
                <td>P.U - HT</td>
                <td>MONTANT HT</td>
                <td>MONTANT TTC</td>
            </tr>
        </thead>

        <tbody>
            {% for produit in facture.commande.produits %}
                <tr>
                    <td class="color">{{ produit.reference }}</td>
                    <td class="color">{{ produit.quantity }}</td>
                    <td class="color">{{ produit.prixHT }} €</td>
                    <td class="color">{{ produit.prixHT * produit.quantity }} €</td>
                    <td class="color">{{ produit.prixTTC }} €</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <table id="total">
        <tr>
            <td width="110">TOTAL HT :</td>
            <td width="100" class="color">{{ facture.commande.totalHT }} €</td>
        </tr>
            <tr>
                <td width="110">TVA :</td>
                <td width="100" class="color">{{ facture.commande.tva }} €</td>
            </tr>
        <tr>
            <td width="110">TOTAL TTC :</td>
            <td width="100" class="color">{{ facture.commande.totalTTC }} €</td>
        </tr>
    </table>

<footer >
    <br><br><br>
    <br><br><br>
    <hr><br>
    <span class="color strong">{{ adresse }}</span> <p>             {{ telephone }}</p>
</footer>

</body>
</html>