<div class="well">
    <ul class="nav nav-list">
        <li>
            <a href="{{ path('panier') }}">{{ 'cart'|trans }}
                <span class="badge" style="background-color:#880000">
                    {#  transchoice : if 0 or 1 it takesthe first option, second otherwise  #}
                    {{ nbrArticles }}&nbsp;&nbsp;{{ 'articles'|transchoice(nbrArticles) }}
                </span>
            </a>
        </li>
    </ul>
</div>
